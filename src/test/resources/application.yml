spring:
  datasource:
    url: jdbc:h2:mem:confitura-next
    username: confitura-next
    password: confitura-next

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

  profiles:
      active: default

logging:
  level:
    org.hibernate: DEBUG
    org.hibernate.SQL: DEBUG


mandril:
  key: fake
gui:
  host: http://localhost:9090