spring:
  datasource:
    url: jdbc:mysql://localhost/confitura-next
    username: confitura-next
    password: confitura-next
    max-active: 20
    max-idle: 8
    min-idle: 5
    initial-size: 5

  jpa:
    hibernate:
      ddl-auto: update

  profiles:
      active: fake

logging:
  level:
    org.hibernate: INFO
    org.hibernate.SQL: DEBUG

#spring.groovy.template.check-template-location=false



mandril:
  key: fake
gui:
  host: http://localhost:9090